<!doctype html>
<html lang="de">
<head>
  <meta charset="utf-8" />
  <title>Kündigungsbestätigung – {{brand.company}}</title>
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <link rel="stylesheet" href="./_base.css" />
</head>
<body>
<main class="doc">

  <!-- Kopfband -->
  <div class="topband">
    <span class="claim">{{brand.company}}</span>
    {{#if brand.logoUrl}}<img class="brand-logo" src="{{brand.logoUrl}}" alt="{{brand.company}}">{{/if}}
  </div>

  <!-- Empfängeradresse (Anrede über dem Namen) -->
  <div class="addr">
    {{#if customer.parent.salutation}}<span class="line">{{customer.parent.salutation}}</span>{{/if}}
    <span class="line">{{customer.parent.firstName}} {{customer.parent.lastName}}</span>
    {{#if customer.address.street}}<span class="line">{{customer.address.street}}{{#if customer.address.houseNo}} {{customer.address.houseNo}}{{/if}}</span>{{/if}}
    {{#if customer.address.zip}}<span class="line">{{customer.address.zip}}{{#if customer.address.city}} {{customer.address.city}}{{/if}}</span>{{/if}}
  </div>

  <!-- Titel -->
  <h1 class="subject subject-cancel">Kündigungsbestätigung</h1>

  <!-- Kernangaben -->
  <div class="facts facts-cancel">
    {{#if booking.cancellationNo}}<div class="row"><span class="k">Kündigungs-Nr.:</span><span class="v">{{booking.cancellationNo}}</span></div>{{/if}}

    {{#if booking.refInvoiceNo}}
      <div class="row">
        <span class="k">Referenzrechnung:</span>
        <span class="v">
          {{booking.refInvoiceNo}}
          {{#if booking.refInvoiceDate}} vom {{fmtDate booking.refInvoiceDate}}{{/if}}
          {{#if booking.refInvoiceDateDE}} ({{booking.refInvoiceDateDE}}){{/if}}
        </span>
      </div>
    {{/if}}

    {{#if customer.userId}}<div class="row"><span class="k">Kunden-Nr.:</span><span class="v">{{customer.userId}}</span></div>{{/if}}
    <div class="row"><span class="k">Datum:</span><span class="v">{{fmtDate details.cancelDate}}</span></div>
  </div>

  <div class="intro intro-regret">
    Wir bedauern, dass Sie unser Angebot
    <strong>
      {{#if booking.offerTitle}}
        {{courseOnly booking.offerTitle}}
      {{else if booking.offerType}}
        {{courseOnly booking.offerType}}
      {{else}}
        {{courseOnly booking.offer}}
      {{/if}}
    </strong>
    {{#if booking.venue}} am Veranstaltungsort <strong>{{booking.venue}}</strong>{{/if}}<br/>
    nicht mehr nutzen möchten.
  </div>

  <div class="intro intro-confirmation">
    Hiermit bestätigen wir den Erhalt Ihrer Kündigung<br/>
    vom&nbsp;<strong>{{#if details.requestDate}}{{fmtDate details.requestDate}}{{else}}{{fmtDate details.cancelDate}}{{/if}}</strong>
    {{#if details.endDate}}&nbsp;zum&nbsp;<strong>{{fmtDate details.endDate}}</strong>{{/if}}
    für <strong>{{customer.child.firstName}} {{customer.child.lastName}}</strong>.
  </div>

  {{#if details.reason}}
    <div class="section-title">Grund</div>
    <div class="notes"><p>{{details.reason}}</p></div>
  {{/if}}

  <!-- Footer -->
  <div class="footer-spacer"></div>
  <footer class="footer">
    <table class="footer-table" cellspacing="0" cellpadding="0" role="presentation">
      <tr>
        <td class="footer-col brand">
          <strong>Münchner Fussball Schule NRW</strong><br/>
          Hochfelder Straße 33<br/>
          47226 Duisburg<br/>
          Deutschland
        </td>
        <td class="footer-col meta">
          <span class="k">Inhaber:</span> <span class="v">Selcuk Kocyigit</span><br/>
          <span class="k">Tel.:</span> <span class="v">0176-43203362</span><br/>
          <a href="mailto:s.kocyigit@muenchner-fussball-schule.ruhr">s.kocyigit@muenchner-fussball-schule.ruhr</a><br/>
          <a href="https://muenchner-fussball-schule.ruhr">https://muenchner-fussball-schule.ruhr</a>
        </td>
        <td class="footer-col bank">
          <span class="k">Bank:</span> <span class="v">Commerzbank</span><br/>
          <span class="k">IBAN:</span> <span class="v nowrap">DE13350400380595090200</span><br/>
          <span class="k">BIC:</span> <span class="v nowrap">COBADEFFXXX</span>
        </td>
      </tr>
    </table>
  </footer>

</main>
</body>
</html>











